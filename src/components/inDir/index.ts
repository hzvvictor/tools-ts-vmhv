import read from "./read";
import { del, getStats, move, rename } from "../inFile";

export {
  read,
  rename,
  move,
  del,
  getStats,
}
export default {
  read,
  rename,
  move,
  del,
  getStats,
}
