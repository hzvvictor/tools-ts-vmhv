import read from "./read";
import { del, getStats, move, rename } from "../inFile";

const functions = {
  read,
  rename,
  move,
  del,
  getStats,
}
export {
  functions as default,
  read,
  rename,
  move,
  del,
  getStats,
}
