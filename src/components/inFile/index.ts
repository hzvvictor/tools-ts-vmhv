import { del, getStats, move, rename } from "../inDirAndFile/actions";
import write from "./write";

export {
  write,
  del,
  move,
  rename,
  getStats,
}
export default {
  write,
  del,
  move,
  rename,
  getStats,
}
